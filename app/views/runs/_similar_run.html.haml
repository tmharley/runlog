%tr.text-center
  %td=link_to local_date_time_string(run.start_time), run
  %td #{'%.2f' % run.distance} mi
  %td
    - if run.weather_type
      = build_weather_icon(run)
    - unless run.temperature.nil?
      #{run.temperature}&deg;F
  %td #{run.elev_gain} ft
  %td #{'%.3f' % similarity}